import React, { Component } from 'react';
import './Title.css';


class Title extends Component {
    render() {
        return (
            <header>
                Google Search Book
            </header>
        );
    }
}

export default Title;